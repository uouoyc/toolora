NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Absolute site origin used for canonical URLs, sitemap, and robots.
# Unset means a local run and falls back to http://localhost:3001.
# Set this to the public production origin when deploying.
# NEXT_PUBLIC_SITE_URL=https://toolora.tools
